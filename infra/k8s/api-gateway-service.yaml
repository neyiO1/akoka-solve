apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: akoka-solve-prod
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP
